<%= form_with(model: busker_profile) do |form| %>
  <% if busker_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(busker_profile.errors.count, "error") %> prohibited this busker_profile from being saved:</h2>

      <ul>
        <% busker_profile.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3">
    <%= form.label :name, class:"col form-label" %>
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :bio, class:"col form-label" %>
    <%= form.text_area :bio, class:"form-control" %>
  </div>

  <div class="actions text-center mt-5">
    <%= form.submit class:"btn btn-info" %>
  </div>
<% end %>
